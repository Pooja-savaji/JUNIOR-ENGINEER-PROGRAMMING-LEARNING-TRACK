def add(a, b):
    return a + b

def average(numbers):
    return sum(numbers) / len(numbers)

def maximum(numbers):
    return max(numbers)

numbers = [10, 20, 30]

print("Sum:", add(10, 20))
print("Average:", average(numbers))
print("Maximum:", maximum(numbers))
